node_modules/
dist/
.env
*.log
.DS_Store
coverage/
.prisma/
packages/api/prisma/*.db
# NOTE: migrations/ is intentionally NOT gitignored.
# Render runs `prisma migrate deploy` on startup and needs the migration files.
